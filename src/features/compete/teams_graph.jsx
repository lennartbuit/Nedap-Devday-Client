import React from "react";
import DetectionResultGraph from "../../components/detection_result_graph";

const TeamsGraph = ({ results }) => <DetectionResultGraph results={results} />;

export default TeamsGraph;
